import { createContext } from 'react'
import type { Activity, Expense, Highlight, Memory, PackingItem, ReefSighting, TripDay } from '../types'
export interface DataValue { tripDays:TripDay[]; sightings:ReefSighting[]; memories:Memory[]; highlights:Highlight[]; packing:PackingItem[]; expenses:Expense[]; loading:boolean; error:string|null; refresh():Promise<void>; saveSighting(value:Omit<ReefSighting,'id'> & {id?:string}):Promise<void>; deleteSighting(id:string):Promise<void>; saveMemory(value:Omit<Memory,'id'|'media'|'thumbnail'|'image'> & {id?:string},file?:File,onProgress?:(progress:number)=>void):Promise<void>; deleteMemory(id:string):Promise<void>; saveTripDay(value:TripDay):Promise<void>; saveActivity(value:Omit<Activity,'id'>):Promise<void>; saveHighlight(value:Omit<Highlight,'id'>):Promise<void>; deleteHighlight(id:string):Promise<void>; savePacking(value:Omit<PackingItem,'id'> & {id?:string}):Promise<void>; deletePacking(id:string):Promise<void>; saveExpense(value:Omit<Expense,'id'>):Promise<void>; deleteExpense(id:string):Promise<void> }
export const DataContext = createContext<DataValue | null>(null)
