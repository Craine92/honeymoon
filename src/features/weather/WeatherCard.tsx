import { CloudSun, Droplets, Wind } from 'lucide-react'
import { Link } from 'react-router-dom'
import { weatherPresentation } from './weatherCodes'
import { useWeather } from './useWeather'
import { getTripPhase } from '../../hooks/useTripPhase'
export function WeatherCard(){const{data,loading,error,stale}=useWeather();const today=data?.daily[0];const condition=data?weatherPresentation(data.current.weatherCode):null;const phase=getTripPhase();const label=phase.phase==='during'||phase.phase==='arrival'?'WETTER HEUTE':'AKTUELL AUF IRU VELI';const update=data?new Intl.DateTimeFormat('de-DE',{hour:'2-digit',minute:'2-digit'}).format(new Date(data.updatedAt)):'';return <Link to="/weather" className="feature-card weather-card real-weather" aria-label="Wetterdetails für Iru Veli öffnen">{loading&&!data?<><CloudSun size={34}/><div><p className="eyebrow">{label}</p><h3>Wetter wird geladen</h3></div></>:data?<><span className="weather-symbol">{condition?.icon}</span><div><p className="eyebrow">{label}</p><h3>{Math.round(data.current.temperature)} °C · {condition?.label}</h3><p>{Math.round(data.current.apparentTemperature)} °C gefühlt · {today?.rainProbability??0} % Regen</p><small><Droplets/> {data.current.humidity} % <Wind/> {Math.round(data.current.windSpeed)} km/h{stale?` · Stand ${update}`:''}</small></div></>:<><CloudSun size={34}/><div><p className="eyebrow">INSELWETTER</p><h3>{error||'Wetterdaten gerade nicht verfügbar'}</h3><p>Tippen, um es erneut zu versuchen</p></div></>}</Link>}
